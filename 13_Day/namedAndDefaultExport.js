import {add, subtract, multiply, divide} from './namedAndDefault.js';
import sum from './namedAndDefault.js';
const first = 1;
const second = 2;
console.log(add(first, second)); // 3
console.log(subtract(first, second)); // -1
console.log(multiply(first, second )); // 2
console.log(divide(first, second )); // 0.5


console.log(sum(first, second)); // 3